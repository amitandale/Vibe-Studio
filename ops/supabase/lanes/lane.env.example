# Example Supabase lane environment configuration.
# Copy this file to ops/supabase/lanes/<lane>.env and replace the placeholder values.
# Passwords and secrets that should not be committed belong in ops/supabase/lanes/credentials.env.

COMPOSE_PROJECT_NAME=supa-example
LANE=example
VOL_NS=example

# Postgres connection values used by Supabase services and host tooling.
PGHOST=127.0.0.1
PGPORT=5432
PGHOST_PORT=5432
PGDATABASE=postgres
PGUSER=supabase_admin

# Supabase edge runtime configuration.
EDGE_ENV_FILE=../supabase/functions/.env.example
EDGE_PORT=9999

# Gateway configuration.
KONG_HTTP_PORT=8000

# JWT secrets (replace with production-ready values before deploying).
JWT_SECRET=replace-me-with-jwt-secret
ANON_KEY=replace-me-with-anon-key
SERVICE_ROLE_KEY=replace-me-with-service-role-key
