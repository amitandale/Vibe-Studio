# DO NOT COMMIT ACTUAL ENV FILES WITH SECRETS
# Supabase Lane Environment Template
#
# Identity
COMPOSE_PROJECT_NAME={{COMPOSE_PROJECT_NAME}}
LANE={{LANE}}
VOL_NS={{VOL_NS}}

# PostgreSQL Connectivity
PGHOST={{PGHOST}}
# Port allocation per lane: main=5433, work=5434, codex=5435
PGPORT={{PGPORT}}
PGDATABASE={{PGDATABASE}}
PGUSER={{PGUSER}}
PGPASSWORD={{PGPASSWORD}}

# Service Ports and Edge Runtime
# Kong HTTP ports: main=8101, work=8102, codex=8103
KONG_HTTP_PORT={{KONG_HTTP_PORT}}
# Edge runtime ports: main=9901, work=9902, codex=9903
EDGE_PORT={{EDGE_PORT}}
EDGE_ENV_FILE={{EDGE_ENV_FILE}}

# Supabase Secrets (replace with production-grade values)
JWT_SECRET={{JWT_SECRET}}
ANON_KEY={{ANON_KEY}}
SERVICE_ROLE_KEY={{SERVICE_ROLE_KEY}}
